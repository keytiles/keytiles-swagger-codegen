openapi: 3.0.1

info:
  version: '1.0'
  title: Test object definitions

paths: {}

components:

  schemas:

    FruitEnum:
      type: string
      enum: [apple, orange, mango]
      nullable: true

      